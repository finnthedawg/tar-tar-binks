#ifndef HIERARCHY_H // Making sure the header files are not included twice
#define HIERARCHY_H

/* display heirarchy of all objects in metaVector */
void display_hierarchy_from_archive (std::vector <struct Metadata> &metaVector);

#endif
